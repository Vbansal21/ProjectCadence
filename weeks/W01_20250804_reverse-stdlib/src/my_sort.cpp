#include "my_sort.hpp"
#include <cstdio>
#include <cstdlib>

template <class RandomIt, class Compare>
void my_sort(RandomIt first, RandomIt last, Compare comp) {
  std::fputs("[my_sort] TODO: implement (pick algo; validate iterator category)\n", stderr);
  std::abort();
}

// Explicit instantiations can go here later if you split decl/def.
